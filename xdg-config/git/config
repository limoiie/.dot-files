[user]
  email = limo.iie4@gmail.com
  name = limoiie

[commit]
  template = ~/.config/git/commit-template

[credential]
  helper = cache

[filter "lfs"]
  clean = git-lfs clean -- %f
  smudge = git-lfs smudge -- %f
  process = git-lfs filter-process
  required = true

[pager]
  diff = bat -ldiff
[core]
	autocrlf = input
